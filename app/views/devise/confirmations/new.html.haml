- provide(:title, 'Повторная отправка письма с подтверждением e-mail')

.page-header
  %h2 Повторная отправка письма с подтверждением e-mail
= form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post }) do |f|
  = devise_error_messages!
  .field
    = f.label :email
    = f.email_field :email, :autofocus => true
  .actions
    = f.submit "Отправить повторно письмо с подтверждением"
= render "devise/shared/links"